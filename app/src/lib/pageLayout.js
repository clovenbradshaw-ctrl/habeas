export const US_LETTER_PAGE_WIDTH_PX = 816; // 8.5in @ 96dpi
export const US_LETTER_PAGE_HEIGHT_PX = 1056; // 11in @ 96dpi
export const PAGE_INNER_MARGIN_PX = 96; // 1in @ 96dpi

export const US_LETTER_CONTENT_WIDTH_PX = US_LETTER_PAGE_WIDTH_PX - (PAGE_INNER_MARGIN_PX * 2);
